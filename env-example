# Copy to .env and fill values. Never commit secrets to source control.

# Public RPC url used as fallback by the app (read-only)
REACT_APP_RPC_URL=https://cloudflare-eth.com

# Optional: discovery / analytics
REACT_APP_SENTRY_DSN=

# Optional: Chain configuration
REACT_APP_CHAIN_ID=1

# Don't put private keys / mnemonics here. Use GitHub secrets for CI or a secrets manager.
