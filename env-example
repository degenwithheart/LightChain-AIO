# Production-ready environment example. Copy to .env (do NOT commit .env).
REACT_APP_RPC_URL=https://mainnet.infura.io/v3/YOUR_INFURA_PROJECT_ID
REACT_APP_CHAIN_ID=1
REACT_APP_SENTRY_DSN=
# Do not store private keys or mnemonics in env files. Use a secrets manager.
